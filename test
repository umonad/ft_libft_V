sise_t  ft_strlcpy(void *dst, char *src, sise_t limit)
{   
    int i = 0;
    int len = 0;
    unsigned char *str;

    str = (unsigned char*)dst;

    (unsigned char *)dst[]

    while(str[len])
        len++;
    while(limit-1 > 0 && str)
}